<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cive Lampus</title>
    
</head>
<body>

    <header>
        <h1> <a href="/home">Cive Lampus</a></h1>
        <nav>
            <% if(user) { %>
            <% if(user.role == "admin") { %>
                <a href="/teachers">Teachers</a>
                <a href="/students">Students</a>
                <a href="/users">Users</a>
            <% } if (user.role == "teacher") { %>
                <a href="/students">Students</a>
            <% } %>
            <%}  %>

            <% if(user) { %>
                <a href="/profile" class="profile-link">Profile</a>
                <form method="GET" action="/api/logout" class="logout-form">
                    <button type="submit" class="logout-button">Logout</button>
                </form>
            <% } else { %>
                <a href="/login" class="login-link">Login</a>
            <% } %>
        </nav>
    </header>

</body>
</html>

<style>
    body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        background-color: #f6f8fa;
        color: #24292f;
        margin: 0;
        padding: 0;
    }

    header {
        background-color: #24292f;
        color: white;
        padding: 20px;
    }

    h1 {
        margin: 0;
        font-size: 28px;
        font-weight: 600;
    }
    a{
       text-decoration: none;
       color: white;
    }

    nav {
        display: flex;
        gap: 20px;
        margin-top: 10px;
    }

    nav a {
        color: #c0ccda;
        text-decoration: none;
        font-size: 14px;
        font-weight: 500;
    }

    nav a:hover {
        color: #0366d6;
    }

    nav form {
        margin-left: auto;
    }

    button {
        background-color: #f6f8fa;
        border: 1px solid #e1e4e8;
        color: #0366d6;
        padding: 6px 12px;
        font-size: 14px;
        cursor: pointer;
        border-radius: 6px;
        transition: background-color 0.2s;
    }

    button:hover {
        background-color: #e1e4e8;
    }

    .user-info {
        margin-top: 20px;
        display: flex;
        align-items: center;
        gap: 10px;
    }

    .user-info p {
        margin: 0;
        font-size: 14px;
    }

    .user-info a {
        color: #0366d6;
    }

    .login-link {
        color: #0366d6;
        text-decoration: none;
    }
</style>